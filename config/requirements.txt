# 现有依赖
requests
openai-whisper
openai
srt
tqdm
transformers
torchaudio
librosa
# 新增依赖
opencv-python  # 用于视频帧提取
matplotlib  # 用于音频可视化
soundfile  # 用于音频处理

# 实时会议记录依赖 - 新功能
pyaudio  # 实时音频捕获
faster-whisper  # CPU优化的Whisper
pyannote.audio  # 说话人分离
torch  # PyTorch支持
numpy  # 数组处理

# 性能优化依赖 - 新增
psutil  # 系统监控和内存管理
colorama  # 终端颜色输出
pyyaml  # YAML配置文件支持

# 音频处理增强
scipy  # 科学计算
librosa  # 音频分析
soundfile  # 音频文件读写

# 并发处理
concurrent-futures  # 并发处理支持

# 数据处理
pandas  # 数据处理和分析
